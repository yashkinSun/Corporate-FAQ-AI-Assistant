Вот **обновленная памятка (Cheat Sheet)** для вашего проекта. Сохраните её как `README_UPDATED.md` или в базу знаний команды. Она учитывает переход на строгую типизацию, миграции и управление памятью.

-----

# 🚀 Памятка по эксплуатации и доработке (v2.0 Post-Patches)

## 1. Новый алгоритм развертывания (Deployment)

Старый метод «просто `docker-compose up`» больше не гарантирует полную работоспособность из-за базы данных.

**Стандартная процедура обновления:**

1.  Обновить код (`git pull` или копирование файлов).
2.  Пересобрать контейнеры (обязательно, чтобы обновились зависимости и конфиги):
    ```bash
    docker-compose up -d --build
    ```
3.  **ОБЯЗАТЕЛЬНО:** Накатить миграции БД:
    ```bash
    docker exec -it botvopros-telegram alembic upgrade head
    ```
    *(Без этого шага новые индексы не создадутся, а будущие изменения схемы вызовут ошибки).*

-----

## 2. Работа с Конфигурацией (`config.py`)

Мы внедрили Pydantic. Теперь конфиг строгий, но требует внимательности при доработке.

  * **⚠️ Ловушка «candidates»:**
    Если вы добавляете новую настройку в класс `Settings`, вы **обязаны** добавить её имя строкой в список `candidates` внутри функции `_collect_env()`.
      * *Почему:* Мы собираем переменные вручную, чтобы не тянуть лишние зависимости. Если не добавить в список — Pydantic не увидит переменную из `.env`, даже если она там есть.
  * **Исчезли "тихие" дефолты:**
    Настройки БД (`PG_USER`, `PG_HOST` и т.д.) больше не имеют значений по умолчанию. Если их нет в `.env` — бот **мгновенно упадет** при старте.
      * *Совет:* Перед запуском на новом сервере запускайте `python3 config.py` (или тестовый скрипт), чтобы проверить валидность `.env` до старта контейнеров.
  * **Типизация ADMIN_IDS:**
    Валидатор сам чистит пробелы. Запись `ADMIN_IDS=123, 456` корректна. Но запись `ADMIN_IDS=admin` уронит бота.

-----

## 3. База данных и Миграции (Alembic)

  * **Перенос на новый сервер:**
    Файл `alembic.ini` теперь должен лежать в корне проекта **на хосте**. Он копируется внутрь при сборке. Без него миграции не заработают.
  * **Правило создания новых миграций:**
    Если вы будете создавать новые миграции (например, добавите таблицу), убедитесь, что в новом файле `down_revision` указывает на ID предыдущей миграции (сейчас это `0002` / или хеш от Codex). Цепочка не должна прерываться.
  * **Рассинхрон (Emergency Fix):**
    Если Alembic снова «потеряется» (ошибка `Can't locate revision`), используйте команду Stamp для фиксации текущего состояния:
    ```bash
    docker exec -it botvopros-telegram alembic stamp <ID_ПОСЛЕДНЕЙ_РЕАЛЬНОЙ_МИГРАЦИИ>
    ```

-----

## 4. Память и Производительность

  * **Очистка памяти (TTL):**
    История уверенности (Confidence History) теперь живет в RAM 1 час. Очистка происходит **при каждом входящем сообщении**.
      * *На будущее:* Если нагрузка вырастет до >10,000 активных пользователей в час, функцию `_cleanup_confidence_history` нужно будет убрать из хендлера и перенести в отдельный фоновый процесс (Celery/Cron), чтобы не замедлять ответы бота.
  * **Восстановление контекста:**
    После перезагрузки контейнера бот «вспоминает» контекст пользователя, подтягивая последние 5 оценок из БД. Это нормально, если после рестарта бот сразу предлагает эскалацию «проблемному» клиенту.

## 5. Логи и Мониторинг

  * **Новые "Ошибки" в логах:**
    Вы можете видеть INFO-сообщения: `Thinking indicator message was removed...`.
    Это **нормальное поведение** (Graceful Degradation). Это значит, что система успешно отловила удаление сообщения и отправила ответ новым сообщением. Не нужно считать это багом.
  * **Безопасность:**
    Если в логах при старте висит `RuntimeWarning: JWT_SECRET_KEY использует значение по умолчанию` — не игнорируйте это на проде. Смените ключ в `.env`.
